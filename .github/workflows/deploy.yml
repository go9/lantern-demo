name: Deploy Demo

on:
  push:
    branches: [main]
  pull_request:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: go9/lantern-demo-app

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.18"
          otp-version: "28.0"

      - name: Install deps
        run: mix deps.get

      - name: Compile (catches errors before Docker build)
        run: mix compile --warnings-as-errors
        env:
          MIX_ENV: test

  deploy:
    needs: test
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

      - name: Deploy to Flicker
        env:
          FLICKER_TOKEN: ${{ secrets.FLICKER_TOKEN }}
        run: |
          APP_ID=$(curl -fsS "https://flickercloud.com/api/v1/apps" \
            -H "Authorization: Bearer $FLICKER_TOKEN" \
            | jq -r '.apps[] | select(.slug=="lantern-demo") | .id')
          curl --fail-with-body -sS -X POST "https://flickercloud.com/api/v1/apps/$APP_ID/deploy" \
            -H "Authorization: Bearer $FLICKER_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"image\":\"${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}\"}"
